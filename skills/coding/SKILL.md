# Coding workflow

Use the fixed sequence: inspect, plan, create `agent/<task>` branch/worktree, edit, test through an approved command profile, review the staged diff, commit, request push approval, open a Draft PR, and report. Never modify main/master directly, force-push, merge, edit `.git/config`, run destructive reset/clean, or request repository administration/secrets/workflow permissions.
